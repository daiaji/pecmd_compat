# 2026-07-07 官方文档二次核验

核验目标：把已抽取的 `chunks/*.md` 与官方 `PECMD帮助文档.txt` 再次对照，检查是否存在命令标题遗漏、附录标题遗漏、隐藏功能遗漏。

## 核验方法

- 重新从官方文档抽取所有 `命令│` 标题。
- 将官方标题与 `features.json` 中的 `id` 和 `aliases` 做集合对照。
- 对隐藏功能密集段做抽样复核：`_SUB`、`ENVI/SET`、`EXEC`、`MOUN`、`REGI`、`SOCK`、`TABL`、`WAIT`。
- 校验 `features.json` JSON 语法和对象统计。

## 发现并修正

- `features.json` 原先没有保留部分官方标题别名，已补 `aliases`：`类`、`窗口`、`函数`、`位置`、`大小`、`层级`、`COME`、`NOTE`、`ENVI`、`SET`、`NAME`、`FNAM`、`THREAD`、`THRD`、`并行符`、`DLL`、`Vkeylist`、`fenqutype`、`disktype`、`ICOLIST`、`DIYSIZE`、`DIYPE`、`致谢`、`许可` 等。
- `features.json` 新增 `thanks_notes`，用于承接官方 `致谢` 标题，避免自动标题对照误报。
- `001-global-to-envi.md` 补充 `_SUB` 备注细节：名称不能重复/近似、透明窗口影响 GIF、`Visible` 映射 `SW_SHOW*`、无标题窗口 Alt 拖动、动态命令查询依赖 `QueryCmd`。
- `001-global-to-envi.md` 补充 `ENVI/SET` 细节：`EnviMode=0/1` 语义、空白保留、递归解释、PE 变量销毁/共享、线程复制、值传递/引用传递和二进制 0。
- `005-appendices-and-meta.md` 补充官方附录标题别名：`Vkeylist`、`fenqutype`、`disktype`、`ICOLIST`、`DIYSIZE`、`DIYPE`。
- `005-appendices-and-meta.md` 新增 `致谢` 小节，作为来源追踪项。
- `002-exec-to-page.md` 补充 `EXEC` 细节：`-waiti`、`-ex1`、`-exe`、`-nfb`、clone 日志条件、`-no64`、服务内嵌参数、CAB 子文件、临时文件安全删除、非数字资源名、前导冲突规则、文档模式引号规则。
- `002-exec-to-page.md` 补充 `MOUN` 细节：`OnlyApp`、`SetOnlyApp`、`setbootcfg`、`getbootcfg`、`Server`、标志文件任一/全部匹配、卷标/UUID 正则、UDM 设备标识/签名、VHD 分区校正、分区表缓存、FBWF 顺序要求、自动挂载注册表/INI 配置。

## 自动对照结果

- 官方 `命令│` 标题经 `id/aliases` 映射后无缺失。
- `features.json` 当前对象数：138。
- 文档块仍连续覆盖官方第 `1-3673` 行。

## 仍需人工注意

- 本次核验确认“对象级标题”和抽样高风险段落无明显遗漏，但 `ENVI @...`、GUI 控件属性、UDM/VHD 生态仍有大量细粒度参数，后续生成 `matrix.md` 时应按 chunk 原文逐项映射，不要只看 `features.json` 对象级索引。
- 旧源码没有参与本次核验；本次仅以官方帮助文档为准。
